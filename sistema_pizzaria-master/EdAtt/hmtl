<!-- edit_pizza.html -->
<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <title>Editar Pizza</title>
</head>
<body>
    <h1>Editar Pizza</h1>
    <div id="mensagem"></div>
    <form id="editPizzaForm">
        <input type="hidden" id="pizzaId" value="{{ pizza.id }}">
        <div>
            <label for="name">Nome da Pizza:</label>
            <input type="text" id="name" value="{{ pizza.name }}" required>
        </div>
        <div>
            <label for="description">Descrição:</label>
            <textarea id="description" required>{{ pizza.description }}</textarea>
        </div>
        <div>
            <label for="price">Preço:</label>
            <input type="number" id="price" value="{{ pizza.price }}" required>
        </div>
        <button type="submit">Atualizar Pizza</button>
    </form>

    <script src="edit_pizza.js"></script>
</body>
</html>
